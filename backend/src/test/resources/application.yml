spring:
  datasource:
    url: jdbc:h2:mem:devops_ai_test;MODE=MySQL;DATABASE_TO_UPPER=false
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false

app:
  jwt:
    secret: test-secret-test-secret-test-secret-test
    expires-seconds: 7200
